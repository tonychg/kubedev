manifests: true
destination:
  name: in-cluster
  namespace: kube-system
helm:
  repoURL: https://helm.cilium.io
  targetRevision: 1.19.1
  chart: cilium
syncPolicy:
  automated:
    enabled: true
    prune: false
    selfHeal: false
  syncOptions:
    - Validate=true
    - CreateNamespace=true
